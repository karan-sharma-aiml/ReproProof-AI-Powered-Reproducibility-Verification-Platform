# Project Overview

ReproProof AI is an enterprise reproducibility verification control plane. It analyzes repositories, evaluates research evidence, orchestrates specialized agents, diagnoses execution risk, produces explainable scores, and exports judge-ready reports.

The project is designed around provider-neutral ports so local demos work without credentials while production deployments can inject LLM, OCR, vector, sandbox, Redis, telemetry, database, and cloud implementations.
